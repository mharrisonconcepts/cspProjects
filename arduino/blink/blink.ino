void setup() {
  // put your setup code here, to run once:
  pinMode(13, OUTPUT); // Sets pin 13 as an output for this program

}

void loop() {
  // put your main code here, to run repeatedly:
  digitalWrite(13, HIGH); // turns LED on
  delay(600);  // Specifies amount of time to run digitalWrite command
  digitalWrite(13, LOW);  // Turns LED off
  delay(100);   // Specifies amount of time to run digitalWrite command
  digitalWrite(13, HIGH); // turns LED on
  delay(600);  // Specifies amount of time to run digitalWrite command
  digitalWrite(13, LOW);  // Turns LED off
  delay(100);  // Specifies amount of time to run digitalWrite command
  digitalWrite(13, HIGH); // turns LED on
  delay(200);  // Specifies amount of time to run digitalWrite command
  digitalWrite(13, LOW);  // Turns LED off
  delay(100);  // Specifies amount of time to run digitalWrite command
   digitalWrite(13, HIGH); // turns LED on
  delay(200);  // Specifies amount of time to run digitalWrite command
  digitalWrite(13, LOW);  // Turns LED off
  delay(100);  // Specifies amount of time to run digitalWrite command
   digitalWrite(13, HIGH); // turns LED on
  delay(600);  // Specifies amount of time to run digitalWrite command
  digitalWrite(13, LOW);  // Turns LED off
  delay(100);  // Specifies amount of time to run digitalWrite command
}
